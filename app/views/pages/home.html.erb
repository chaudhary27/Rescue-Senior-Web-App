<div class="home-callout text-center container-fluid">
  <h1>Welcome to RescueSenior</h1>
  <h3>We are a team of students building location aware health emergency solutions for senior citizens.</h3>
</div>
<div class="container home-signup-links">
  <div class="row">
    <% if user_signed_in? %>
    <div class="col-md-6">
      <div class="well">
        <div class="text-center">
          <h2 class="text-center">Connect your health tracking device</h2>
          <% if current_user.profile %>
          <%= link_to "Edit your profile", edit_user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-lg btn-block' %>
          <%= link_to "View your profile", user_path(current_user.id), class: 'btn btn-default btn-lg btn-block' %>
          <% else %>
          <p>Create your profile to connect health device.</p>
          <%= link_to "Create profile and enable health tracking", new_user_profile_path(user_id: current_user.id), class: 'btn btn-default btn-lg btn-block' %>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">
          Enable Location Tracking
        </h2>
        <p>This will allow us to send help in case of emergency.</p>
        <%= link_to "Enable Beacon Location", '#', class: 'btn btn-default btn-lg btn-block' %>
      </div>
    </div>
    <% else %>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Connect Health Tracking</h2>
        <h4 class="text-center">Sign up and create a profile to get started.</h4>
        <br>
        <%= link_to "Sign up & create a profile", new_user_registration_path, class: 'btn btn-primary btn-lg btn-block' %>
      </div>
    </div>
    <div class="col-md-6">
      <div class="well">
        <h2 class="text-center">Enable Location Beacon</h2>
        <h4 class="text-center">This will help us send help in case of emergency.</h4>
        <br>
        <%= link_to "Allow location tracking via beacon", '#', class: 'btn btn-success btn-lg btn-block' %>
      </div>
    </div>
    <% end %>
  </div>
</div>

<div class="col-xs-10 col-xs-offset-1 features text-center">
  <div class="col-sm-4">
    <%= image_tag('demo.png') %>
    <h3>Step 1</h3>
  </div>
  <div class="col-sm-4">
    <%= image_tag('demo.png') %>
    <h3>Step 2</h3>
  </div>
  <div class="col-sm-4">
    <%= image_tag('demo.png') %>
    <h3>Step 3</h3>
  </div>
</div>
